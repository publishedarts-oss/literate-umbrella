[build]
buildCommand = "cd global-arbitrage-engine && bun install"

[deploy]
startCommand = "cd global-arbitrage-engine && bun src/index.ts"
healthcheckPath = "/"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
